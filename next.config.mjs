/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  // Enable static HTML export to the `out/` directory
  output: 'export',
};

export default nextConfig;
